import CameraAssistant from "./CameraAssistant";

function App() {
  return (
    <div
      style={{
        minHeight: "100vh",
        backgroundColor: "#0f172a",
        color: "#e5e7eb",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <CameraAssistant />
    </div>
  );
}

export default App;
